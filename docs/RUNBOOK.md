# Runbook

## Docs / Session handoff

The session-handoff skill lives at `.agents/skills/session-handoff/`. It maintains these docs: `README.md`, `docs/HANDOFF.md`, `docs/SESSION_LOG.md`, `docs/DECISIONS.md`, `docs/RUNBOOK.md`.

Get current UTC timestamp for handoff/log entries: `date -u '+%Y-%m-%d %H:%M UTC'`
